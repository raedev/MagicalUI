<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raedev.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo">
        <activity
            android:name=".bottomsheet.BottomSheetDemoActivity"
            android:exported="true"
            android:label="BottomSheet示例">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".bottomsheet.BasicBottomSheetActivity"
            android:exported="true"
            android:label="基础示例" />
        <activity
            android:name=".bottomsheet.RecycleViewBottomSheetActivity"
            android:exported="true"
            android:label="下拉刷新列表示例" />
    </application>

</manifest>